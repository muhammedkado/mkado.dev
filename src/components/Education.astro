---
import { education, languages } from '../data/site';
---

<section id="education">
  <div class="wrap">
    <div class="facts">
      <div>
        <h3>Education</h3>
        <p class="big">{education.degree}</p>
        <p class="sub">{education.school}, {education.period}</p>
      </div>
      <div>
        <h3>Languages</h3>
        <ul>
          {languages.map((l) => (
            <li><span>{l.name}</span><span>{l.level}</span></li>
          ))}
        </ul>
      </div>
    </div>
  </div>
</section>
